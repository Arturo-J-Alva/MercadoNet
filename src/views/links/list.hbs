<main class="fondo2">
    <div class="container p-4 mx-auto">
    <div class="row">
        
        {{#each productos}}
        {{!-- THIS MUESTRA LOS DATOS QUE ESTÁ RECORRIENDO  #each (links) --}}
         <div class="col-md-3">
            <div class="card text-center bg-transparent">
                <div class="card-body ">
                    {{!-- <a href="{{ }}" target="_blank"> --}}
                        <h2>{{productname}}</h2>
                        <h3 class="card-title text-uppercase">
                            {{price}} soles
                        </h3> 
                    </a>
                    <p class="m-2">{{description}}</p>
                    <p>{{timeago created_at}}</p>
                    <a href="/links/delete/{{id}}" class="btn btn-danger">Eliminar</a>
                    <a href="/links/edit/{{id}}" class="btn btn-secondary">Editar</a>
                    
            </div>
        </div>
    </div>

    {{else}}
    <div class="col-md-4 mx-auto">
        <div class="card card-body text-center">
            <p>No tienes productos guardados...</p>
            <a href="/links/add">Crea uno!</a>
        </div>
    </div>
    {{/each}}
</div>
</main>