<main class="fondo1">
    <div class="container-fluid p-4 mx-auto">
    <div class="row">
        
        {{#each NuevaTabla}}
        {{!-- THIS MUESTRA LOS DATOS QUE ESTÁ RECORRIENDO  #each (links) --}}
         <div class="col-md-3">
            <div class="card text-center bg-transparent text-dark">
                <div class="card-body">
                    {{!-- <a href="{{ }}" target="_blank"> --}}
                        <h2>{{productname}}</h2>
                        <h3 class="card-title text-uppercase">
                            {{price}} soles
                        </h3>
                    </a>
                    <p class="m-2">{{description}}</p>
                    <h4>Vendedor:</h4> 
                    <h4>{{empresa}}</h4>
                    <p>{{timeago created_at}}</p>
                    <a href="#" class="btn btn-danger">Comprar</a>
                    
                    
            </div>
        </div>
    </div>

    {{else}}
    <div class="col-md-4 mx-auto">
        <div class="card card-body text-center">
            <p>Lo sentimos, no hay productos disponibles por ahora...</p>
            {{!-- <a href="/links/add">Crea uno!</a> --}}
        </div>
    </div>
    {{/each}}
</div>
</main>